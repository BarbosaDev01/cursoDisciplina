@model IEnumerable<WebApplication1.Models.instituicao>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.instnome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.insendereco)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.instnome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.insendereco)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.instid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.instid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.instid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
