@model WebApplication1.Models.departamento

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>departamento</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="depid" />
            <div class="form-group">
                <label asp-for="depnome" class="control-label"></label>
                <input asp-for="depnome" class="form-control" />
                <span asp-validation-for="depnome" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="instid" class="control-label"></label>
                <input asp-for="instid" class="form-control" />
                <span asp-validation-for="instid" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
